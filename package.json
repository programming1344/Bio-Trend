{
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "backend": "node backend/server.js",
    "backend:watch": "node --watch backend/server.js",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "predeploy": "npm run build",
    "deploy": "powershell -Command \"Remove-Item -Path dist/assets/bte-video-17-02-2026.* -Force -ErrorAction SilentlyContinue\"; gh-pages -d dist"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
